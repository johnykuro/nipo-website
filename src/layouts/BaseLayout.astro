---
import "@fontsource/playfair-display/400.css";
import "@fontsource/playfair-display/500.css";
import "../styles/global.css";
import { siteConfig } from "../config/site";
import { buildPageStructuredData, siteNoIndex } from "../config/seo";
import SiteHeader from "../components/SiteHeader.astro";
import SiteFooter from "../components/SiteFooter.astro";
import CookieConsent from "../components/CookieConsent.astro";

interface Props {
  title: string;
  description: string;
  image?: string;
  imageAlt?: string;
  noIndex?: boolean;
  overlayHeader?: boolean;
}

const {
  title,
  description,
  image = "/images/social-card.png",
  imageAlt = "NIPO logo beside salmon sushi rolls on a bronze platter, Newcastle Quayside",
  noIndex = false,
  overlayHeader = false,
} = Astro.props;
const site = new URL(siteConfig.canonicalUrl);
const canonical = new URL(Astro.url.pathname, site);
const socialImage = new URL(image, site);
const structuredData = noIndex ? undefined : buildPageStructuredData(siteConfig, Astro.url.pathname, title, description);
const jsonLd = structuredData
  ? JSON.stringify(structuredData).replace(/</g, "\\u003c")
  : undefined;
---

<!doctype html>
<html lang="en-GB">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#15231F" />
    <meta name="color-scheme" content="dark light" />
    <meta name="description" content={description} />
    <meta
      name="robots"
      content={
        (noIndex || siteNoIndex)
          ? "noindex, nofollow"
          : "index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1"
      }
    />
    {!noIndex && <link rel="canonical" href={canonical} />}
    <link rel="icon" type="image/svg+xml" href="/brand/nipo-mark.svg" />
    <link rel="preconnect" href="https://challenges.cloudflare.com" />

    <meta property="og:type" content="website" />
    <meta property="og:locale" content="en_GB" />
    <meta property="og:site_name" content="NIPO" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={imageAlt} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />
    <meta name="twitter:image:alt" content={imageAlt} />

    {jsonLd && <script is:inline type="application/ld+json" set:html={jsonLd} />}

    <title>{title}</title>
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to content</a>
    <SiteHeader overlay={overlayHeader} />
    <slot />
    <SiteFooter />
    <CookieConsent />
    <script>import "../scripts/site";</script>
  </body>
</html>
